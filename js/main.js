require(['.requireConfig', 'jquery'], function ($) {
    $(document).ready(function() {
        // whatever our main view ends up being
        // PS guys Alvaro is awesome
        require(['../app/login']);
    });
});
